# List of numbers
numbers = [1, 2, 3, 4, 5]

# Looping over the sequence using a for loop
for number in numbers:
    square = number ** 2  # Calculate the square of the number
    print(f"The square of {number} is {square}")
